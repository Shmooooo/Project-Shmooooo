Find the gcd of two integers a, b. Additionally finds x, y such that ax + by = gcd(a, b).

Not that well documented but you can figure it out. 

Works correctly with signs. gcd(0, 0) not tested, don't expect it to work.
